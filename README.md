# Prediksi Klasifikasi dan Regresi Titanic

Repository ini berisi implementasi machine learning menggunakan dataset Titanic untuk melakukan:

- Klasifikasi menggunakan metode **K-Nearest Neighbor (KNN)**
- Regresi menggunakan **Regresi Linear Berganda**

## 📂 Dataset
Dataset yang digunakan adalah **Titanic Dataset** dalam format CSV.

## 🛠 Tools & Bahasa
- Python
- Jupyter Notebook
- Library: pandas, numpy, scikit-learn, matplotlib

## 📁 Struktur Folder
- `data/` → berisi dataset Titanic (`titanic.csv`)
- `models/` → berisi notebook KNN dan Regresi
- `README.md` → dokumentasi project

## ▶ Cara Menjalankan
1. Buka file notebook (`.ipynb`)
2. Jalankan cell secara berurutan
3. Pastikan dataset berada di folder `data/`
